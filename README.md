Build a minimal elegant `docker-compose` stack exposing an HTTP service around `nvidia/speakerverification_en_titanet_large` for speaker embedding extraction, enrollment, verification, and identification. Return a clean appropriate JSON response for what the model provides. Ensure that a health-check informs when the model is fully loaded and the api is ready to service requests. Compute embeddings on GPU, perform cosine-based matching, and return predicted speaker labels, scores, and an `unknown` outcome when below threshold. Include a smoke test that starts the stack, enrolls test_person.zip, requests identification on test.opus, verifies scores and a predicted label are returned, and shuts the stack down cleanly. Keep runtime settings and secrets in `.env`. Credentials that may be needed: `HF_TOKEN`. Optimize for the local GPU, assume it is dedicated for this service and no concurrency is needed.
